# Robot Design Skill

## 出图网关最高优先级

所有机器人皮肤相关出图都必须先通过 `scripts/robot_skin_gateway.py`。本 skill 是规则源，网关是执行入口；不能绕过网关直接调用 imagegen、裁图脚本或手写脚本把结果保存为项目资产。

- 出图前必须先运行 `preflight`，并确认它的状态是 `ready`。
- 新需求只能增加或细化约束，不能隐式取消本 skill 的全局约束。
- 没有 preflight 记录时，不允许生成或保存机器人皮肤候选图。
- 没有通过 `audit` 的图，只能视为失败稿或候选稿，不能进入 `assets/skins`。
- `assets/skins` 只允许保存通过 `final_parts` preflight 和 audit 的最终皮肤。
- “确认稿”也必须遵守来源、颜色、形状、方向端点、内外侧、掌心/手背和关节归属约束；不能因为不是最终 PNG 就降低标准。
- 如果网关结论与即时需求冲突，以网关和本 skill 的全局硬约束为准。

## 设计确认流程硬约束

在创建任何一套新机甲皮肤时，必须先出完整设计图，等待用户确认后，才允许进入拆分切片阶段。

1. 第一阶段只生成设计图，不生成可绑定拆件 PNG。
   - 设计图必须同时包含主视图和侧视图。
   - 主视图用于确认整体比例、头身关系、配色、材质、装甲语言、关节语言和正面轮廓。
   - 侧视图用于确认所有后续拆分部件的唯一角度基准，尤其是手臂、腿部、脚部、手掌/拳头的阅读方向。
   - 设计图可以是整机参考图或设计表，但不能替代最终拆件。
   - 用户没有明确确认设计图通过之前，禁止输出 `head.png`、`torso.png`、`outer_*`、`inner_*` 等最终拆件。

2. 第二阶段才拆分出图。
   - 只有在用户明确确认主视图和侧视图设计没问题后，才能切分或重绘绑定用部件。
   - 拆分图必须以已确认侧视图为唯一角度基准。
   - 所有最终拆件都必须是侧视图或严格服务于侧视骨骼绑定的侧向贴图，不允许混入主视图、三分之四视图、俯视或正视部件。
   - 即使头部和躯干来自主视图确认过的设计语言，最终用于绑定的拆件仍必须符合侧视皮肤的统一朝向和骨骼阅读方向。
   - 拆分阶段不能重新设计机甲，只能把已确认的主视图/侧视图设计语言转换成可绑定部件。
   - 进入拆分前必须先做“来源合格性判定”：逐个列出 `head`、`torso`、`outer_*`、`inner_*` 是否有可用的侧视参考、掌心/手背参考、外侧/里侧参考和上下左右端点参考。任一必需 slot 缺少可靠参考时，必须停止并说明缺失项，不能继续生成完整目录。
   - 固定画布不是合格证明。把设计图局部裁剪、旋转、缩放后放进固定画布，仍然属于直接裁图，不等于重绘成绑定用部件。
   - 不允许为了凑齐整套皮肤而混用来源：不能一部分从当前设计图裁剪，一部分从旧皮肤复制，一部分凭空补画，除非用户明确要求做临时占位版本，并且输出目录/说明必须标记为占位，不得称为合格最终拆件。
   - 如果当前设计图只提供主视 + 单侧侧视，但没有里侧侧视、掌心侧手、手背侧手或某些端点参考，就只能先输出“缺失参考清单”或“绑定部件表确认稿需求”，不能直接输出 `inner_*` 等最终 PNG。

3. 如果用户只说“创建一套机甲”“做一套皮肤”“根据 skill 生成机甲”，默认执行第一阶段：先出主视图 + 侧视图设计图，并等待确认。
   - 不得自动继续到拆件。
   - 不得用“我先顺手拆好”作为默认行为。
   - 如需继续拆件，必须由用户在确认设计图后再次明确要求。

4. 用户要求“根据设计图重新出部件图”时，也不能默认跳过闸门。
   - 先判断该设计图是不是已经足够作为最终拆件来源。
   - 如果设计图只是整机展示、手部变体展示、风格参考或局部参考，必须先说明它不能直接产出完整最终拆件。
   - 正确下一步是生成或要求一张“绑定用部件表确认稿”：所有需要的 slot 独立摆放、侧视角度统一、外侧/里侧明确、手部掌心/手背明确、端点和上下左右方向明确。
   - 只有这个绑定用部件表也被确认后，才允许进入最终 PNG 输出。

5. 头部或局部设计被用户确认后，仍然必须先回到整机确认图。
   - 如果中途单独迭代了头像、手部、脚部、肩甲等局部方案，下一步必须先生成“整机主视图 + 整机侧视图”的新版设计确认稿。
   - 只有用户明确确认这个新版整机主视图 + 侧视图通过后，才允许进入拆分切片。
   - 不能把局部确认等同于整机确认；例如用户确认了头像，不代表可以直接生成 `head.png` 或整套切片。
   - 如果不确定用户希望继续做整机确认稿还是进入切片，必须先询问用户，不能凭空推进流程。

## 单件出图与 TODO 流程

生成 14 个绑定部件时，禁止一次性出完整 14 部件大图。必须按 slot 一张一张生成、检查、锁定。

- 进入部件生成前必须先创建 14 项 TODO，固定顺序为：`head`、`torso`、`outer_upper_arm`、`inner_upper_arm`、`outer_forearm`、`inner_forearm`、`outer_hand`、`inner_hand`、`outer_thigh`、`inner_thigh`、`outer_shin`、`inner_shin`、`outer_foot`、`inner_foot`。
- 每个 TODO 项只能对应一个 slot 的单独出图；不能在同一次 imagegen 调用中同时生成多个 slot。
- 每张单件图都必须单独执行来源、方向端点、内外侧、掌心/手背、关节自上而下、颜色和参考设计一致性审计。
- 单件审计成功后，该 slot 标记为 `approved/locked`，后续迭代不得重出、重绘、覆盖或“顺手优化”该 slot，除非用户明确要求修改这个已锁定 slot。
- 单件审计失败后，只允许重出失败的这个 slot；不得重出整套 14 件，也不得重出已经 approved/locked 的 slot。
- 如果多个 slot 失败，也必须逐个按 TODO 重出和审计；不能把失败项合并成一张多部件图。
- 14 个 slot 全部 approved/locked 后，才允许进入“拼版预览”或最终 PNG 固定画布输出；拼版只用于展示和核对，不是新的设计生成机会。
- 拼版或最终输出阶段如果发现某个 slot 失败，只回到该 slot 的 TODO 项重出；不能推翻整套已通过部件。
- 任何“一次性出 14 张/一张图里包含 14 个部件”的流程，只能作为历史失败方式，不得作为后续默认流程。
- 用户要求重新生成某个 slot，或指出某个角度、方向、形状、颜色、比例问题时，该问题立即成为该 slot 的必须项失败原因；下一轮出图和审计必须逐字检查该问题，不能把它当作可选建议或风格偏好。
- 每次单件生成后必须对照用户本轮指定的参考图做显式自查；如果无法说明输出如何满足用户指出的失败点，不允许标记 approved/locked。

## 参考图姿态到绑定方向映射

参考图中的局部部件可能是展示姿态，不一定已经等同于最终 slot 的绑定方向。使用参考图局部造型前，必须先判定它在参考图里的实际姿态方向，再重绘到当前 slot 的绑定方向。

- 不允许把参考图里“向上/向下/斜向”的拳头、手掌、脚或肢体直接当成目标 slot 的“向左/向右/横向/纵向”绑定件。
- 手部尤其必须先判断参考图拳头的腕口、拳面、拇指、四指主体分别在哪个方向，再映射到 `outer_hand` 或 `inner_hand` 的绑定基准。
- `outer_hand` 绑定基准仍是腕口在右、拳面/指节在左；如果参考图外侧拳头是向上的展示拳头，只能继承它的造型、颜色、指节结构和拇指关系，不能继承它的画面方向。
- `inner_hand` 绑定基准仍是腕口在左、掌心/手指在右，且拇指/掌根侧在下；参考图掌心手如果是竖向展示，也必须重绘到该绑定方向。
- 躯干、脚和四肢同理：参考图中的展示角度只提供造型语言；最终单件必须匹配当前 slot 的端点方向和侧视朝向。
- 审计时必须明确写出“参考图姿态方向是什么”和“输出 slot 绑定方向是什么”；两者未正确映射即失败。

## 切片输出硬约束

生成或切分可换贴图皮肤时，部件 PNG 不能按透明 bbox 自由裁剪成不同尺寸，必须输出到固定预设画布中。透明背景保留，装甲内容在画布内按对应骨骼方向居中或按需要贴近端点摆放。

当前默认皮肤部件画布尺寸必须保持为：

- `head.png`: 266x259
- `torso.png`: 248x279
- `outer_upper_arm.png` / `inner_upper_arm.png`: 305x77
- `outer_forearm.png` / `inner_forearm.png`: 343x80
- `outer_hand.png` / `inner_hand.png`: 296x163
- `outer_thigh.png` / `inner_thigh.png`: 78x261
- `outer_shin.png` / `inner_shin.png`: 76x268
- `outer_foot.png` / `inner_foot.png`: 290x221

所有新皮肤的 `skin.json` 必须以 `assets/skins/default_skin.json` 为模板：

- 只能按新皮肤目录更新 `name`、`display_name`、`base_dir` 和各 slot 的 `texture` 文件名。
- `offset`、`scale`、`rotation`、`mirror`、`layer`、`opacity` 必须继承默认模板，除非用户明确要求重新校准。
- 当前默认模板来自 `Sport Robot` 这套图片的可用绑定参数；后续生成脚本也必须复制这个模板，而不是手写一套新的默认值。

外侧/里侧成对部件必须严格对称约束：

- 外侧/里侧同类部件的 PNG 画布尺寸必须完全一致，不能因为切片 bbox 不同导致宽高不一致。
- 外侧/里侧上臂、外侧/里侧前臂、外侧/里侧手、外侧/里侧大腿、外侧/里侧小腿、外侧/里侧脚都必须使用同一套尺寸、同一缩放基准、同一留白逻辑。
- 成对部件的可见装甲体量应接近；外侧/里侧颜色和图案可以不同，但不允许一个明显放大、另一个明显缩小。
- 外侧/里侧脚必须使用同样的 290x221 画布，鞋底高度、脚尖位置、脚踝区域留白应保持一致。
- 外侧/里侧拳头必须使用同样的 296x163 画布，拳头朝向可以相反，但腕口、拳面和整体体量必须对称。

所有拆件必须使用同一个角色朝向：

- 先确定整套皮肤的统一侧视朝向，例如全部面向画面左侧，或全部面向画面右侧；确定后所有拆件都必须沿用这个朝向。
- 不允许同一套皮肤里头部朝左、脚朝右、手臂又像另一侧；这会导致拼装后角色朝向混乱。
- 外侧/里侧 slot 不是让图片互相反向的理由。`outer_*` 与 `inner_*` 可以表现外侧/里侧差异，但必须属于同一个角色朝向下看到的对应侧面。
- 不能为了对称而把外侧手背拳头简单镜像成另一只手；镜像只能解决外侧/里侧形状，不能把外侧变成掌心侧。
- 侧视图中的里侧手部 slot 如果按当前骨骼/动作需要显示掌心，就必须使用掌心侧手掌或掌心侧拳头，不能使用外侧手背图。
- 生成部件表时，必须明确标注或在内部判定每个手部版本是 `palm side` 还是 `back side`；输出到皮肤 slot 前必须选对版本。

骨骼贴图方向必须与编辑器绑定逻辑一致：

- `Sport Robot` 是当前绑定端点基准；新皮肤不仅要匹配画布尺寸，也必须匹配每个 slot 的左/右/上/下端点语义。
- 上臂和前臂贴图必须是横向长图，近端关节在左，远端关节在右。
  - `outer_upper_arm.png` / `inner_upper_arm.png`: 左端是肩部近端圆关节或肩端连接件，右端是肘部远端接口。
  - `outer_forearm.png` / `inner_forearm.png`: 左端是肘部近端接口，右端是腕部远端接口。
  - 不允许把手臂切片左右翻转成“腕口在左、肘/肩在右”的版本；即使造型好看，绑定后也会反向。
- 大腿和小腿贴图必须是纵向长图，近端在上，远端在下。
  - `outer_thigh.png` / `inner_thigh.png`: 上端是髋部近端接口，下端是膝部远端接口。
  - `outer_shin.png` / `inner_shin.png`: 上端是膝部近端接口，下端是踝部远端接口。
  - 不允许把腿部上下倒置；膝盖、踝口、护甲尖角和高光都必须支持“上近端、下远端”的阅读。
- 手部贴图必须是横向图，并按 `Sport Robot` 的 slot 方向放置：
  - `outer_hand.png`: 腕口在右，拳面/指节在左，是外侧手背拳头。
  - `inner_hand.png`: 腕口在左，拳面/指节在右，是里侧掌心拳头或掌心手。
  - `inner_hand.png` 还必须匹配 `Sport Robot` 的上下读法：拇指/掌根侧在画面下方，四指卷曲指节主体在画面右侧，手背/掌甲上缘在画面上方；不能只因为腕口在左、拳面在右就判定合格。
  - 外侧手和里侧手可以左右相反，但不能随意互换；`outer_hand.png` 做成腕口在左即不合格，`inner_hand.png` 做成腕口在右即不合格，`inner_hand.png` 掌心/拇指上下颠倒也不合格，除非用户明确要求重新校准整套绑定模板。
- 脚部贴图必须是侧视横向图，并按 `Sport Robot` 的源 PNG 方向放置：
  - `outer_foot.png` / `inner_foot.png`: 脚尖在左，脚跟和脚踝结构在右，鞋底横向稳定。
  - 不允许画成脚尖在右、脚踝在左的反向脚；这类图即使是侧视，也不符合当前 skin.json 绑定基准。
  - 脚踝必须位于脚掌上方靠后位置，脚尖、鞋底、后跟不能在画布里左右漂移。
- 头部和躯干也必须保持同一源 PNG 朝向：
  - `head.png`: 侧脸朝左，后脑/颈部在右下，不能生成朝右头或三分之四头。
  - `torso.png`: 颈口在上、骨盆/腰口在下，胸甲/前侧朝左，背侧在右，不能上下倒置或前后反向。
- 如果原始设计图里的部件是竖放的，但目标 slot 需要横向贴图，必须旋转到骨骼方向后再放入固定画布。

### 上下左右端点反例

`red_power_generated_14` 是当前反面教材：它的问题不是画布尺寸，而是部分 slot 的端点语义偏离 `Sport Robot` 绑定基准。

- 脚部反例：脚尖朝右、脚踝/后跟在左或中部，会和当前 `Sport Robot` 源 PNG 的“脚尖在左、脚踝/后跟在右”相反。
- 手部反例：`outer_hand.png` 做成腕口在左、拳面在右，会和当前外侧手“腕口在右、拳面在左”的绑定方向相反。
- 里侧手反例：`inner_hand.png` 虽然腕口在左、拳面在右，但拇指/掌根侧跑到画面上方，也会和 `Sport Robot` 的里侧掌心手上下读法相反；这种情况必须垂直镜像或重画，不能判定为方向正确。
- 腿部反例：大腿或小腿如果缺少明确的髋/膝/踝端点，或者护甲形状让上端像踝口、下端像膝口，即使整体是竖图也不合格。
- 手臂反例：上臂/前臂如果把近端和远端左右互换，或用正视圆孔、侧向 socket 掩盖端点错误，即使横向长图尺寸正确也不合格。
- 评审时必须先问“这个 slot 的左端/右端/上端/下端分别是什么关节”，不能只问“看起来是不是侧视”和“尺寸是否正确”。

切片自检必须额外检查：

1. 是否已经有用户确认通过的主视图 + 侧视图设计图；没有确认图就拆件即不合格。
2. 所有 PNG 是否符合预设尺寸。
3. 外侧/里侧成对部件尺寸是否完全一致。
4. 外侧/里侧成对部件在画布中的位置、缩放、留白是否一致。
5. 手臂是否横向、腿是否纵向、脚和手是否符合对应骨骼方向。
6. 每个 slot 的左右/上下端点是否匹配 `Sport Robot` 绑定基准：手臂近端在左远端在右，腿部近端在上远端在下，脚尖在左脚踝在右，外侧手腕口在右，里侧手腕口在左，且里侧手的拇指/掌根侧在下、四指主体在右。
7. 所有拆件是否严格来自已确认侧视图的角度基准；混入主视、正视、俯视或三分之四视角即不合格。
8. 所有拆件是否属于同一个侧视朝向；出现头、脚、手、躯干朝向互相矛盾即不合格。
9. 里侧手部 slot 需要掌心侧时，是否真的使用掌心侧手掌/拳头；用外侧手背拳头即不合格。
10. 是否误把透明 bbox 当作最终画布，导致每个切片大小不同；出现即不合格。

用于生成或评审当前参考设计图对应机甲角色的 2D 动画拆件图。核心目标是：部件可以绑定到现有火柴人骨骼上，且外形、颜色、材质、角度、线条语言都延续输入的原始设计图，而不是重新设计一套机器人。

## 最高优先级

1. 原始机器人设计图是唯一主视觉基准。
   - 先从设计图中归纳主色、辅色、点缀色、关节/连接件颜色和材质，再按这个归纳生成或评审。
   - 保持设计图中的装甲语言、边缘形状、描边方式、渲染风格和材质质感；如果原图不是圆角装甲或黑色描边，就不要强行套用圆角装甲或黑色描边。
   - 不使用与原设计不一致的简化 SVG、图标风、随机重配色或随机重设计作为最终风格。

2. 只补缺失视角，不重新发明部件。
   - 需要的是能装到现有火柴人骨骼上的切件。
   - 部件数必须跟骨骼部件对应：手臂三件、腿三件，手掌/拳头作为手部状态版本。

3. 输出后必须按约束自查，不能相信生成器自称的内容。
   - 视觉上不是里侧，就算提示词写了 inner side 也不合格。
   - 视觉上有三分之四角度，就算生成器说是 side view 也不合格。

## 里侧/外侧判断

- 外侧不是主视图左右手臂。
- 里侧也不是把外侧镜像。
- 里侧是朝向身体中心线的一面，需要从主视图和侧视图共同推断。
- 里外两侧的颜色必须跟随主视图中可见的颜色逻辑，不能凭感觉重配色。

### 颜色与材质规则

- 颜色规则必须从当前参考设计图动态推导，不固定为某一套配色。
- 先判断每种颜色在原设计中的角色：主装甲色、局部护甲色、点缀色、发光/传感器色、关节色、缝隙/连接件色。
- 生成缺失视角时，只能把某种颜色放到原设计支持的结构位置上；不能因为画面空就随机增加色块。
- 不允许把外侧的大面积护板颜色直接复制到里侧前臂、里侧小腿或其他本应露出不同面的部位，除非参考图明确支持这种延续。
- 关节、指节、缝隙、机械连接件的颜色和材质也必须跟随参考设计；如果原设计使用深色连接件，就保持深色，如果原设计使用其他材质，也按原设计归纳。
- 当前蓝白橙运动型机器人只是一个实例：白色多为主装甲，蓝色多为边缘/外壳/窄条，橙色多为小型嵌件，深石墨/黑灰多为关节和连接件。这个例子不能作为所有设计图的固定规则。

## 角度约束

手臂、腿部和脚部必须保持跟原始侧视图一致的阅读方向。

不合格信号：

- 部件看起来是三分之四角度。
- 能看到明显的正面宽度、顶部平面或两个侧边。
- 关节处出现“外圆 + 偏心小孔/内孔/侧向 socket/圆环孔”的形状。
- 这种关节形状表示部件朝向错误，不能通过把关节强行改成正圆来修补；必须重新生成或重画正确角度的部件。

脚部规则：

- 脚必须是明确侧视图。
- 脚尖方向必须匹配当前 `Sport Robot` 绑定基准：源 PNG 中脚尖在左，脚踝/后跟在右。
- 不允许画成正视鞋头、俯视鞋面或三分之四鞋。

## 关节颜色与结构规则

- 手臂、腿部的关节本体颜色必须来自参考设计中的关节/连接件语言。
- 不允许把关节本体画成随机颜色，也不允许把装甲主色、点缀色误画成关节核心颜色，除非原设计明确就是这样。
- 护甲边缘、外壳、腕口、踝口、鞋边装饰可以使用设计图支持的装甲色，但不能让它们看起来侵入或替代关节核心。
- 如果腕口或踝口的装甲色看起来像关节颜色混乱，应优先改成“参考图支持的外壳颜色 + 参考图支持的连接核心颜色”的结构。

## 关节自上而下原则

关节归属必须沿骨骼树自上而下：父级部件可以携带连接子级的“向下关节”，子级部件不能携带回连父级的“向上关节”。判断时先确定该 slot 的近端/远端，再看关节是否属于从身体根部往末端传递的方向。

- 头部可以有向下连接颈部的关节或颈口；躯干不能带向上连接头部的完整关节。
- 躯干可以有向下连接大腿/髋部的关节；大腿不能带向上连接躯干/髋部的完整关节。
- 大腿可以有向下连接小腿的膝部关节；小腿不能带向上连接大腿的完整膝关节。
- 小腿可以有向下连接脚部的踝部关节；脚不能带向上连接小腿的完整踝关节。
- 上臂可以有向下连接前臂的肘部关节；前臂不能带向上连接上臂的完整肘关节。
- 前臂可以有向下连接手部的腕部关节；手部不能带向上连接前臂的完整腕关节。
- 对手臂三部件和腿部三部件都适用同一规则：越靠身体根部的部件负责向末端方向的关节，越靠末端的部件只表现自己的装甲、接口或被连接区域，不能重复表现向上的完整关节。
- 外侧/里侧都必须遵守该归属原则；不能因为 inner/outer 差异或镜像关系，把里侧部件画成带向上关节。

## 手部约束

手部必须单独约束，不能只写“手臂里侧”。

- 手部版本必须区分掌心侧和手背侧；不能把外侧手背拳头当成掌心侧拳头使用。
- 在侧视拆件皮肤中，如果里侧手部骨骼需要显示掌心侧，则 `inner_hand.png` 必须是掌心侧拳头或掌心侧手掌。
- `outer_hand.png` 和 `inner_hand.png` 可以朝向相反以匹配同一角色侧视骨骼，但二者不能都是外侧手背图。
- 判断拳头时先看是否能看到掌甲、掌纹/掌心面、拇指压在拳面前侧；如果只看到手背护甲和指背，就是外侧手背拳头，不合格。

### 掌心侧握拳

- 必须是掌心侧拳头，不是外侧手背拳头。
- 掌甲颜色必须跟随参考图中的掌心/手甲主色，不固定为白色。
- 四个卷曲指节必须可读，颜色和材质跟随参考图中的手指/指节设计。
- 拇指必须压在拳面或指节前侧，形成五指握拳。
- 不允许三指、四指、爪子、圆球拳、整块无指节拳套。
- 不允许把外侧手背的大面积装甲直接画到掌心侧；掌心侧颜色和结构必须从参考图的里侧/掌心逻辑推断。

### 掌心侧张开手

- 必须五指：四根分节手指 + 一根侧拇指。
- 掌甲主色可见，并且来自参考图。
- 手指和指节颜色、材质跟随参考图。
- 腕口、窄边、护甲壳颜色只能使用参考图支持的颜色角色。

### 掌纹式机械纹理

为了区分里侧掌心和外侧手背，掌心侧拳头和张开手掌都需要机械掌纹：

- 纹理必须画在参考图支持的掌甲主色区域上。
- 使用 3 到 5 条浅刻线/面板线。
- 形态类似掌纹，但必须是机械刻槽，不是人手皮肤纹。
- 可包含 thumb base arc、横向掌纹线、短分段线槽。
- 不允许随机乱划，不允许画到腕口、手指或参考图不支持的材质区域上。

## 生成提示词要点

提示词必须明确列出：

- 原始设计图是唯一主参考。
- 当前只生成一个指定 slot，不能生成 14 件总表、其他 slot、整机或额外变体。
- 当前 slot 在 14 项 TODO 中的名称、状态和本轮目标；如果该 slot 已 approved/locked，不得再次出图。
- 用户上一轮指出的失败点是什么，本轮如何作为必须项修正。
- 参考图中该部件的展示姿态方向是什么，目标 slot 的绑定方向是什么，以及本轮如何从参考姿态重绘到绑定方向。
- 先归纳当前设计图的配色、材质、线条、装甲形状和关节语言，再写入生成约束。
- 忽略错误的旧图和简化 SVG 风格。
- 只生成拆件，不生成整机。
- 左侧区域放手臂/手部，右侧区域放腿/脚部。
- 如果生成掌心侧手掌或拳头，必须带机械掌纹；如果生成外侧手背版本，则不能把掌心纹理误画到手背上。
- 禁止关节处出现偏心孔、内孔、侧向 socket、圆环孔。
- 禁止手臂关节颜色乱来，关节本体必须使用参考图中对应的关节/连接件颜色和材质。
- 必须遵守关节自上而下原则：父级部件可以带向下连接子级的关节，子级部件不能带向上回连父级的完整关节。

## 自查清单

每次出图后必须逐项检查：

1. 是否遵守单件 TODO 流程：本次只生成一个 slot，且没有夹带其他 slot、14 件总表、整机或额外变体。
2. 如果该 slot 已 approved/locked，是否错误地重复出图或覆盖；出现即不合格。
3. 用户上一轮指出的失败点是否已逐项修正；未逐项修正即不合格。
4. 是否先判定参考图姿态方向，再正确映射到当前 slot 绑定方向；把参考图展示方向误当成绑定方向即不合格。
5. 是否仍像原始机器人设计图，而不是新风格。
6. 是否只包含需要的切件，没有整机、文字、箭头、标签。
7. 手臂、腿部、脚部是否是正确方向，不是三分之四角度。
8. 关节处是否出现偏心孔/侧向 socket/圆环孔；出现即不合格。
9. 手臂关节本体是否符合参考图中的关节/连接件颜色和材质；随机色或误用装甲色即不合格。
10. 关节是否遵守自上而下归属：头/躯干/上臂/前臂/大腿/小腿可以带向下关节，躯干/大腿/小腿/脚/前臂/手不能带向上回连父级的完整关节。
11. 里侧前臂和里侧小腿是否没有机械复制外侧大面积护板颜色，而是符合参考图推断出的里侧逻辑。
12. 掌心拳头是否五指结构完整，拇指可见，掌甲主色和掌心结构符合参考图。
13. 掌心张开手是否四指加拇指。
14. 掌心是否有机械掌纹线槽，并且线槽只在参考图支持的掌甲区域上。
15. 脚尖方向是否匹配 `Sport Robot` 绑定基准：脚尖在左，脚踝/后跟在右。

如果任一关键项不合格，必须明确指出失败点，不能说“基本正确”。
