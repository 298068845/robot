# Robot Design Skill

用于生成或评审当前蓝白橙机甲角色的 2D 动画拆件图。核心目标是：部件可以绑定到现有火柴人骨骼上，且外形、颜色、角度都延续原始设计图，而不是重新设计一套机器人。

## 最高优先级

1. 原始机器人设计图是唯一主视觉基准。
   - 保持蓝、白、橙、深石墨配色。
   - 保持圆角装甲、黑色描边、半写实卡通机甲渲染。
   - 不使用简化 SVG、图标风、随机黑灰重设计作为最终风格。

2. 只补缺失视角，不重新发明部件。
   - 需要的是能装到现有火柴人骨骼上的切件。
   - 部件数必须跟骨骼部件对应：手臂三件、腿三件，手掌/拳头作为手部状态版本。

3. 输出后必须按约束自查，不能相信生成器自称的内容。
   - 视觉上不是内侧，就算提示词写了 inner side 也不合格。
   - 视觉上有三分之四角度，就算生成器说是 side view 也不合格。

## 内侧/外侧判断

- 外侧不是主视图左右手臂。
- 内侧也不是把外侧镜像。
- 内侧是朝向身体中心线的一面，需要从主视图和侧视图共同推断。
- 内外两侧的颜色必须跟随主视图中可见的颜色逻辑，不能凭感觉重配色。

### 内侧颜色规则

- 白色装甲通常是主面积。
- 蓝色只能在原设计支持的位置出现，例如窄边、护甲边缘、腕口外壳、鞋底/鞋边、少量侧边条。
- 不允许把外侧的大蓝色护板直接搬到内侧前臂或内侧小腿。
- 橙色只允许作为原设计语言中的小型嵌件，不允许随机增加橙色条。
- 深石墨/黑灰用于关节、指节、缝隙、机械连接件。

## 角度约束

手臂、腿部和脚部必须保持跟原始侧视图一致的阅读方向。

不合格信号：

- 部件看起来是三分之四角度。
- 能看到明显的正面宽度、顶部平面或两个侧边。
- 关节处出现“外圆 + 偏心小孔/内孔/侧向 socket/圆环孔”的形状。
- 这种关节形状表示部件朝向错误，不能通过把关节强行改成正圆来修补；必须重新生成或重画正确角度的部件。

脚部规则：

- 脚必须是明确侧视图。
- 脚尖方向必须正确，通常朝右。
- 不允许画成正视鞋头、俯视鞋面或三分之四鞋。

## 关节颜色规则

- 手臂、腿部的关节本体必须是深石墨/黑灰。
- 不允许把关节本体画成蓝色、白色、橙色或随机混色。
- 蓝色可以是护甲边缘、外壳、腕口/鞋边装饰，但不能侵入关节本体。
- 如果腕口或踝口蓝色看起来像关节颜色混乱，应优先改成“蓝色外壳 + 黑灰连接核心”的结构。

## 手部约束

手部必须单独约束，不能只写“手臂内侧”。

### 掌心侧握拳

- 必须是掌心侧拳头，不是外侧手背拳头。
- 白色掌甲必须可见。
- 四个卷曲深石墨指节必须可读。
- 拇指必须压在拳面或指节前侧，形成五指握拳。
- 不允许三指、四指、爪子、圆球拳、黑色整块拳套。
- 不允许出现大面积蓝色手背壳；蓝色只可作为腕口外壳或窄边。

### 掌心侧张开手

- 必须五指：四根分节手指 + 一根侧拇指。
- 白色掌甲可见。
- 深石墨手指和指节。
- 蓝色只在腕口外壳或窄边。

### 掌纹式机械纹理

为了区分内侧掌心和外侧手背，掌心侧拳头和张开手掌都需要机械掌纹：

- 纹理必须在白色掌甲上。
- 使用 3 到 5 条浅刻线/面板线。
- 形态类似掌纹，但必须是机械刻槽，不是人手皮肤纹。
- 可包含 thumb base arc、横向掌纹线、短分段线槽。
- 不允许随机乱划，不允许画到蓝色腕口或手指上。

## 生成提示词要点

提示词必须明确列出：

- 原始设计图是唯一主参考。
- 忽略错误的旧图和简化 SVG 风格。
- 只生成拆件，不生成整机。
- 左侧放手臂/手部，右侧放腿/脚部。
- 手掌和拳头必须是掌心侧，并带机械掌纹。
- 禁止关节处出现偏心孔、内孔、侧向 socket、圆环孔。
- 禁止手臂关节颜色乱来，关节本体必须深石墨/黑灰。

## 自查清单

每次出图后必须逐项检查：

1. 是否仍像原始机器人设计图，而不是新风格。
2. 是否只包含需要的切件，没有整机、文字、箭头、标签。
3. 手臂、腿部、脚部是否是正确方向，不是三分之四角度。
4. 关节处是否出现偏心孔/侧向 socket/圆环孔；出现即不合格。
5. 手臂关节本体是否全是深石墨/黑灰；乱色即不合格。
6. 内侧前臂和内侧小腿是否没有复制外侧大蓝色护板。
7. 掌心拳头是否五指结构完整，拇指可见，白色掌甲可见。
8. 掌心张开手是否四指加拇指。
9. 掌心是否有机械掌纹线槽，并且线槽只在白色掌甲上。
10. 脚尖方向是否正确。

如果任一关键项不合格，必须明确指出失败点，不能说“基本正确”。
