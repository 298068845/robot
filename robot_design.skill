# Robot Design Skill

## 设计确认流程硬约束

在创建任何一套新机甲皮肤时，必须先出完整设计图，等待用户确认后，才允许进入拆分切片阶段。

1. 第一阶段只生成设计图，不生成可绑定拆件 PNG。
   - 设计图必须同时包含主视图和侧视图。
   - 主视图用于确认整体比例、头身关系、配色、材质、装甲语言、关节语言和正面轮廓。
   - 侧视图用于确认所有后续拆分部件的唯一角度基准，尤其是手臂、腿部、脚部、手掌/拳头的阅读方向。
   - 设计图可以是整机参考图或设计表，但不能替代最终拆件。
   - 用户没有明确确认设计图通过之前，禁止输出 `head.png`、`torso.png`、`left_*`、`right_*` 等最终拆件。

2. 第二阶段才拆分出图。
   - 只有在用户明确确认主视图和侧视图设计没问题后，才能切分或重绘绑定用部件。
   - 拆分图必须以已确认侧视图为唯一角度基准。
   - 所有最终拆件都必须是侧视图或严格服务于侧视骨骼绑定的侧向贴图，不允许混入主视图、三分之四视图、俯视或正视部件。
   - 即使头部和躯干来自主视图确认过的设计语言，最终用于绑定的拆件仍必须符合侧视皮肤的统一朝向和骨骼阅读方向。
   - 拆分阶段不能重新设计机甲，只能把已确认的主视图/侧视图设计语言转换成可绑定部件。

3. 如果用户只说“创建一套机甲”“做一套皮肤”“根据 skill 生成机甲”，默认执行第一阶段：先出主视图 + 侧视图设计图，并等待确认。
   - 不得自动继续到拆件。
   - 不得用“我先顺手拆好”作为默认行为。
   - 如需继续拆件，必须由用户在确认设计图后再次明确要求。

## 切片输出硬约束

生成或切分可换贴图皮肤时，部件 PNG 不能按透明 bbox 自由裁剪成不同尺寸，必须输出到固定预设画布中。透明背景保留，装甲内容在画布内按对应骨骼方向居中或按需要贴近端点摆放。

当前默认皮肤部件画布尺寸必须保持为：

- `head.png`: 266x259
- `torso.png`: 248x279
- `left_upper_arm.png` / `right_upper_arm.png`: 305x77
- `left_forearm.png` / `right_forearm.png`: 343x80
- `left_hand.png` / `right_hand.png`: 296x163
- `left_thigh.png` / `right_thigh.png`: 78x261
- `left_shin.png` / `right_shin.png`: 76x268
- `left_foot.png` / `right_foot.png`: 290x221

左右成对部件必须严格对称约束：

- 左右同类部件的 PNG 画布尺寸必须完全一致，不能因为切片 bbox 不同导致宽高不一致。
- 左右上臂、左右前臂、左右手、左右大腿、左右小腿、左右脚都必须使用同一套尺寸、同一缩放基准、同一留白逻辑。
- 成对部件的可见装甲体量应接近；外侧/内侧颜色和图案可以不同，但不允许一个明显放大、另一个明显缩小。
- 左右脚必须使用同样的 290x221 画布，鞋底高度、脚尖位置、脚踝区域留白应保持一致。
- 左右拳头必须使用同样的 296x163 画布，拳头朝向可以相反，但腕口、拳面和整体体量必须对称。

所有拆件必须使用同一个角色朝向：

- 先确定整套皮肤的统一侧视朝向，例如全部面向画面左侧，或全部面向画面右侧；确定后所有拆件都必须沿用这个朝向。
- 不允许同一套皮肤里头部朝左、脚朝右、手臂又像另一侧；这会导致拼装后角色朝向混乱。
- 左右 slot 不是让图片互相反向的理由。`left_*` 与 `right_*` 可以表现外侧/内侧差异，但必须属于同一个角色朝向下看到的对应侧面。
- 不能为了对称而把外侧手背拳头简单镜像成另一只手；镜像只能解决左右形状，不能把外侧变成掌心侧。
- 侧视图中的右侧手部 slot 如果按当前骨骼/动作需要显示掌心，就必须使用掌心侧手掌或掌心侧拳头，不能使用外侧手背图。
- 生成部件表时，必须明确标注或在内部判定每个手部版本是 `palm side` 还是 `back side`；输出到皮肤 slot 前必须选对版本。

骨骼贴图方向必须与编辑器绑定逻辑一致：

- 上臂和前臂贴图必须是横向长图，近端关节在左，远端关节在右。
- 大腿和小腿贴图必须是纵向长图，近端在上，远端在下。
- 手部贴图必须是横向图，腕口到拳面/手尖沿水平方向阅读。
- 脚部贴图必须是侧视横向图，脚踝和脚掌位置稳定，不能被裁成局部鞋面。
- 如果原始设计图里的部件是竖放的，但目标 slot 需要横向贴图，必须旋转到骨骼方向后再放入固定画布。

切片自检必须额外检查：

1. 是否已经有用户确认通过的主视图 + 侧视图设计图；没有确认图就拆件即不合格。
2. 所有 PNG 是否符合预设尺寸。
3. 左右成对部件尺寸是否完全一致。
4. 左右成对部件在画布中的位置、缩放、留白是否一致。
5. 手臂是否横向、腿是否纵向、脚和手是否符合对应骨骼方向。
6. 所有拆件是否严格来自已确认侧视图的角度基准；混入主视、正视、俯视或三分之四视角即不合格。
7. 所有拆件是否属于同一个侧视朝向；出现头、脚、手、躯干朝向互相矛盾即不合格。
8. 右侧手部 slot 需要掌心侧时，是否真的使用掌心侧手掌/拳头；用外侧手背拳头即不合格。
9. 是否误把透明 bbox 当作最终画布，导致每个切片大小不同；出现即不合格。

用于生成或评审当前参考设计图对应机甲角色的 2D 动画拆件图。核心目标是：部件可以绑定到现有火柴人骨骼上，且外形、颜色、材质、角度、线条语言都延续输入的原始设计图，而不是重新设计一套机器人。

## 最高优先级

1. 原始机器人设计图是唯一主视觉基准。
   - 先从设计图中归纳主色、辅色、点缀色、关节/连接件颜色和材质，再按这个归纳生成或评审。
   - 保持设计图中的装甲语言、边缘形状、描边方式、渲染风格和材质质感；如果原图不是圆角装甲或黑色描边，就不要强行套用圆角装甲或黑色描边。
   - 不使用与原设计不一致的简化 SVG、图标风、随机重配色或随机重设计作为最终风格。

2. 只补缺失视角，不重新发明部件。
   - 需要的是能装到现有火柴人骨骼上的切件。
   - 部件数必须跟骨骼部件对应：手臂三件、腿三件，手掌/拳头作为手部状态版本。

3. 输出后必须按约束自查，不能相信生成器自称的内容。
   - 视觉上不是内侧，就算提示词写了 inner side 也不合格。
   - 视觉上有三分之四角度，就算生成器说是 side view 也不合格。

## 内侧/外侧判断

- 外侧不是主视图左右手臂。
- 内侧也不是把外侧镜像。
- 内侧是朝向身体中心线的一面，需要从主视图和侧视图共同推断。
- 内外两侧的颜色必须跟随主视图中可见的颜色逻辑，不能凭感觉重配色。

### 颜色与材质规则

- 颜色规则必须从当前参考设计图动态推导，不固定为某一套配色。
- 先判断每种颜色在原设计中的角色：主装甲色、局部护甲色、点缀色、发光/传感器色、关节色、缝隙/连接件色。
- 生成缺失视角时，只能把某种颜色放到原设计支持的结构位置上；不能因为画面空就随机增加色块。
- 不允许把外侧的大面积护板颜色直接复制到内侧前臂、内侧小腿或其他本应露出不同面的部位，除非参考图明确支持这种延续。
- 关节、指节、缝隙、机械连接件的颜色和材质也必须跟随参考设计；如果原设计使用深色连接件，就保持深色，如果原设计使用其他材质，也按原设计归纳。
- 当前蓝白橙运动型机器人只是一个实例：白色多为主装甲，蓝色多为边缘/外壳/窄条，橙色多为小型嵌件，深石墨/黑灰多为关节和连接件。这个例子不能作为所有设计图的固定规则。

## 角度约束

手臂、腿部和脚部必须保持跟原始侧视图一致的阅读方向。

不合格信号：

- 部件看起来是三分之四角度。
- 能看到明显的正面宽度、顶部平面或两个侧边。
- 关节处出现“外圆 + 偏心小孔/内孔/侧向 socket/圆环孔”的形状。
- 这种关节形状表示部件朝向错误，不能通过把关节强行改成正圆来修补；必须重新生成或重画正确角度的部件。

脚部规则：

- 脚必须是明确侧视图。
- 脚尖方向必须正确，通常朝右。
- 不允许画成正视鞋头、俯视鞋面或三分之四鞋。

## 关节颜色与结构规则

- 手臂、腿部的关节本体颜色必须来自参考设计中的关节/连接件语言。
- 不允许把关节本体画成随机颜色，也不允许把装甲主色、点缀色误画成关节核心颜色，除非原设计明确就是这样。
- 护甲边缘、外壳、腕口、踝口、鞋边装饰可以使用设计图支持的装甲色，但不能让它们看起来侵入或替代关节核心。
- 如果腕口或踝口的装甲色看起来像关节颜色混乱，应优先改成“参考图支持的外壳颜色 + 参考图支持的连接核心颜色”的结构。

## 手部约束

手部必须单独约束，不能只写“手臂内侧”。

- 手部版本必须区分掌心侧和手背侧；不能把外侧手背拳头当成掌心侧拳头使用。
- 在侧视拆件皮肤中，如果右侧手部骨骼需要显示掌心侧，则 `right_hand.png` 必须是掌心侧拳头或掌心侧手掌。
- `left_hand.png` 和 `right_hand.png` 可以朝向相反以匹配同一角色侧视骨骼，但二者不能都是外侧手背图。
- 判断拳头时先看是否能看到掌甲、掌纹/掌心面、拇指压在拳面前侧；如果只看到手背护甲和指背，就是外侧手背拳头，不合格。

### 掌心侧握拳

- 必须是掌心侧拳头，不是外侧手背拳头。
- 掌甲颜色必须跟随参考图中的掌心/手甲主色，不固定为白色。
- 四个卷曲指节必须可读，颜色和材质跟随参考图中的手指/指节设计。
- 拇指必须压在拳面或指节前侧，形成五指握拳。
- 不允许三指、四指、爪子、圆球拳、整块无指节拳套。
- 不允许把外侧手背的大面积装甲直接画到掌心侧；掌心侧颜色和结构必须从参考图的内侧/掌心逻辑推断。

### 掌心侧张开手

- 必须五指：四根分节手指 + 一根侧拇指。
- 掌甲主色可见，并且来自参考图。
- 手指和指节颜色、材质跟随参考图。
- 腕口、窄边、护甲壳颜色只能使用参考图支持的颜色角色。

### 掌纹式机械纹理

为了区分内侧掌心和外侧手背，掌心侧拳头和张开手掌都需要机械掌纹：

- 纹理必须画在参考图支持的掌甲主色区域上。
- 使用 3 到 5 条浅刻线/面板线。
- 形态类似掌纹，但必须是机械刻槽，不是人手皮肤纹。
- 可包含 thumb base arc、横向掌纹线、短分段线槽。
- 不允许随机乱划，不允许画到腕口、手指或参考图不支持的材质区域上。

## 生成提示词要点

提示词必须明确列出：

- 原始设计图是唯一主参考。
- 先归纳当前设计图的配色、材质、线条、装甲形状和关节语言，再写入生成约束。
- 忽略错误的旧图和简化 SVG 风格。
- 只生成拆件，不生成整机。
- 左侧放手臂/手部，右侧放腿/脚部。
- 如果生成掌心侧手掌或拳头，必须带机械掌纹；如果生成外侧手背版本，则不能把掌心纹理误画到手背上。
- 禁止关节处出现偏心孔、内孔、侧向 socket、圆环孔。
- 禁止手臂关节颜色乱来，关节本体必须使用参考图中对应的关节/连接件颜色和材质。

## 自查清单

每次出图后必须逐项检查：

1. 是否仍像原始机器人设计图，而不是新风格。
2. 是否只包含需要的切件，没有整机、文字、箭头、标签。
3. 手臂、腿部、脚部是否是正确方向，不是三分之四角度。
4. 关节处是否出现偏心孔/侧向 socket/圆环孔；出现即不合格。
5. 手臂关节本体是否符合参考图中的关节/连接件颜色和材质；随机色或误用装甲色即不合格。
6. 内侧前臂和内侧小腿是否没有机械复制外侧大面积护板颜色，而是符合参考图推断出的内侧逻辑。
7. 掌心拳头是否五指结构完整，拇指可见，掌甲主色和掌心结构符合参考图。
8. 掌心张开手是否四指加拇指。
9. 掌心是否有机械掌纹线槽，并且线槽只在参考图支持的掌甲区域上。
10. 脚尖方向是否正确。

如果任一关键项不合格，必须明确指出失败点，不能说“基本正确”。
